# Índice de fases

- **Classe:** Engine
- **Papel:** histórico
- **Status:** ativo

Fases numeradas da Pinker, agrupadas por faixa nominal de 50.

## Shards
- `001a050.md` — faixa nominal `001a050`; cobre de `9 - disciplina de pilha da Machine` até `50 - cast explícito (`virar`)`.
- `051a100.md` — faixa nominal `051a100`; cobre de `51 - `peso(tipo)` e `alinhamento(tipo)`` até `100 - remoção mínima complementar de diretório + leitura textual mínima de arquivo em `--run``.
- `101a150.md` — faixa nominal `101a150`; cobre de `101 - escrita textual mínima de arquivo em `--run`` até `150 - coleções e estruturas de dados básicas: escrita mínima por índice em `lista<bombom>` (camada 1 conservadora)`.
- `151a200.md` — faixa nominal `151a200`; cobre de `151 - coleções e estruturas de dados básicas: remoção mínima do fim em `lista<bombom>` (camada 1 conservadora)` até `200 - ergonomia e expressividade: `escolha/caso/padrao` (switch/case)`.
- `201a250.md` — faixa nominal `201a250`; cobre de `201 - ergonomia e expressividade: retorno implícito` até `213 - Eixo B (backend nativo): ABI SysV completa de funções (B2)`.
