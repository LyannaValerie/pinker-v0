pacote main;

carinho principal() -> bombom {
    nova tem_saida: logica = tem_chave("--saida");
    nova saida: verso = pedir_argumento("--saida", "padrao.txt");
    nova modo: verso = pedir_argumento("--modo", "simples");
    falar(tem_saida);
    falar(saida);
    falar(modo);
    mimo 0;
}
