pacote main;

// Fase 124 mantém recorte mínimo: `>=` ainda fora no backend externo.
carinho principal() -> bombom {
    nova muda x: bombom = 0;

    sempre que x >= 2 {
        x = x + 1;
    }

    mimo x;
}
