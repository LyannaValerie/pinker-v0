pacote main;

carinho principal() -> bombom {
    nova saudacao: verso = "oi ";
    nova nome: verso = "Pinker";
    nova frase: verso = juntar_verso(saudacao, nome);
    falar(frase);

    nova tamanho: bombom = tamanho_verso(frase);
    falar(tamanho);

    mimo 0;
}
