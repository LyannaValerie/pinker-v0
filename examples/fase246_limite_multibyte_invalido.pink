pacote main;

carinho principal() -> bombom {
    nova bytes: seta<u8> = alocar(9);
    nova ultimo: seta<u64> = (bytes virar seta<bombom> + 8) virar seta<u64>;
    nova valor: u64 = *ultimo;
    falar(valor);
    mimo 0;
}
