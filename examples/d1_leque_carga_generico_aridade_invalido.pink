pacote main;

// D1 negativo — especialização genérica com aridade errada.
leque Opcao<T> {
    Algum(T),
    Nenhum,
}

apelido OpcaoErrada = Opcao<lista<bombom>, lista<verso>>;

carinho principal() -> bombom {
    mimo 0;
}
