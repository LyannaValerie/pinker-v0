schema = 3
id = "capsula-doc-catalog"
state = "FROZEN"
justification = "Estado completo da cápsula doc-catalog, medido pelo gate capsula_doc_catalog_cartografa_suporte_e_quatro_testes."

[reconstruction]
base_snapshot = "capsula-trama-query"
expected_overrides = 3
expected_exclusions = 2

[measures]
regions = 428
length = 196111
fnv1a64 = "fnv1a64:7765a8d8cead63f4"

[[rules]]
op = "exclude-key"
key = "evidencia.trama.query.nav-map"
expected_matches = 1

[[rules]]
op = "exclude-file"
file = "tests/trama_query_tests.rs"
expected_matches = 8

[[rules]]
op = "override-region"
key = "cli.execucao.entrada"
from_summary = "try_or_exit! encerra falhas operacionais com EXIT_FAILURE; main imprime ajuda em stdout, versão determinística do pacote em stdout, erros de invocação em stderr com EXIT_USAGE e preserva os códigos de domínio ao despachar análise, build, editor, repl, doc, nav e agente."
to_summary = "try_or_exit! extrai um Result::Ok ou imprime o erro renderizado com a fonte e chama std::process::exit(1); main() chama parse_args, e em Err imprime a mensagem e sai com EXIT_USAGE (para doc/nav) ou 1 (demais), senão despacha CliCommand para run_analyze/run_build/run_editor/run_repl/run_doc/run_nav; scan_code chama nav::CodeIndex::scan_repo e sai com 1 em Err; run_nav roteia NavSub para run_nav_mostrar/buscar/listar/sincronizar/verificar."
expect_file = "src/main.rs"
expect_domain = "execucao"
expect_layer = "cli"

[[rules]]
op = "override-region"
key = "cli.nav.consulta"
from_hash = "fnv1a64:5427c227378dad57"
to_hash = "fnv1a64:b071ea7b358669f4"
from_summary = "load_code_catalog lê o catálogo gerado (nav::CodeCatalog::load) sem escrever; run_nav_mostrar extrai e valida uma região pela fonte; run_nav_buscar/listar/mapa consultam somente o catálogo em memória. Mapa seleciona caminho exato ou reutiliza a busca textual integral, agrupa por arquivo e renderiza texto/JSON determinísticos sem ler fontes."
to_summary = "load_code_catalog lê o catálogo gerado (nav::CodeCatalog::load) sem escrever; run_nav_mostrar extrai uma região por chave e, via nav::validate_region, verifica se o marcador/hash da fonte ainda bate com o catálogo antes de imprimir o conteúdo (texto ou JSON), retornando EXIT_SOURCE em divergência; run_nav_buscar e run_nav_listar apenas consultam o catálogo em memória (busca textual e filtro por camada/domínio) e imprimem os resultados — nenhuma das três funções grava em disco."
expect_file = "src/main.rs"
expect_domain = "nav"
expect_layer = "cli"

[[rules]]
op = "override-region"
key = "trama.codigo.consulta"
from_hash = "fnv1a64:c5af4400c7a070c8"
to_hash = "fnv1a64:a60459ec57aa77bb"
from_summary = "Reconstrói o catálogo de código do JSONL versionado e serve as consultas (`mostrar`/`buscar`/`listar`/`mapa`) sem revarrer raízes; `mapa` prioriza caminho literal e reutiliza a busca textual integral. Ao extrair uma região, valida marcadores e hash, recusando drift."
to_summary = "Reconstrói o catálogo de código do JSONL versionado e serve as consultas (`mostrar`/`buscar`/`listar`) a partir das fontes já catalogadas, sem revarrer as raízes de código controladas; ao extrair uma região, valida que os marcadores ainda a delimitam e que o hash do conteúdo confere, recusando drift."
expect_file = "src/nav.rs"
expect_domain = "navegacao"
expect_layer = "trama"
