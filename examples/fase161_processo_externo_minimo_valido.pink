pacote main;

carinho principal() -> bombom {
    nova comando: verso = argumento(0);
    nova codigo: bombom = executar_processo(comando);
    falar(codigo);
    talvez codigo == 0 {
        mimo 0;
    }
    mimo 1;
}
