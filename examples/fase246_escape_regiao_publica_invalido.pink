pacote main;

trazer memoria.alocar;

carinho principal() -> bombom {
    nova bytes: seta<u8> = alocar(8);
    nova fora: seta<u8> =
        (bytes virar seta<bombom> + 64) virar seta<u8>;
    *fora = 77;
    falar(*fora);
    mimo 0;
}
