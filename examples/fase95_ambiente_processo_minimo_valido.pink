pacote main;

trazer ambiente.variavel_ou;
trazer texto.tamanho;

carinho principal() -> bombom {
    nova nome: verso = variavel_ou("PINKER_TEST_ENV_PHASE95", "visitante");
    falar(nome);
    mimo tamanho(nome);
}
