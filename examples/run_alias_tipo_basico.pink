pacote main;

apelido Byte = u8;
apelido Grande = u32;

carinho soma(a: Byte, b: Byte) -> Byte {
    mimo a + b;
}

carinho principal() -> bombom {
    nova x: Byte = soma(40, 2);
    nova y: Grande = 10;
    x;
    y;
    mimo 42;
}
