pacote main;

trazer ambiente.argumento;
trazer processo.executar;

carinho principal() -> bombom {
    nova comando: verso = argumento(0);
    nova codigo: bombom = executar(comando, "--modo=ok");
    falar(codigo);
    talvez codigo == 0 {
        mimo 0;
    }
    mimo 1;
}
