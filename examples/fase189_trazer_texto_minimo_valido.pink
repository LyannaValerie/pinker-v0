pacote main;

trazer texto.aparar;
trazer texto.juntar;


carinho principal() -> bombom {
    nova saudacao: verso = juntar("rosa", " pinker");
    nova limpa: verso = aparar("  texto  ");
    falar(saudacao);
    falar(limpa);
    mimo 0;
}
