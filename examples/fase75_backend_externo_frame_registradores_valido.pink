pacote main;

carinho aplica(x: bombom) -> bombom {
    nova muda y: bombom = x * 3;
    y = y - 2;
    mimo y;
}

carinho principal() -> bombom {
    nova base: bombom = 7;
    nova muda valor: bombom = aplica(base + 1);
    valor = valor * 2;
    mimo valor;
}
