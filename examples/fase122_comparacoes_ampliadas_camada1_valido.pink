pacote main;

carinho diferente_u64_minimo(a: u64, b: u64) -> bombom {
    talvez a != b {
        mimo 122;
    } senao {
        mimo 12;
    }
}

carinho principal() -> bombom {
    nova esquerdo: u64 = 9;
    nova direito: u64 = 7;
    mimo diferente_u64_minimo(esquerdo, direito);
}
