pacote main;

trazer memoria.alocar, liberar;

carinho principal() -> bombom {
    nova bytes: seta<u8> = alocar(16);
    nova interior: seta<u8> = (bytes virar seta<bombom> + 1) virar seta<u8>;
    liberar(interior);
    mimo 0;
}
