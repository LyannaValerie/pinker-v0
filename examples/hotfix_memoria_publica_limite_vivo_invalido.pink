pacote main;

trazer memoria.alocar, liberar;

carinho principal() -> bombom {
    nova primeira: seta<u8> = alocar(134217728);
    nova segunda: seta<u8> = alocar(134217729);
    liberar(primeira);
    liberar(segunda);
    mimo 0;
}
