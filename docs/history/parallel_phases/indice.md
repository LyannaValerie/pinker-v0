# Índice de fases paralelas

- **Classe:** Engine
- **Papel:** histórico
- **Status:** ativo

Rodadas paralelas registradas, agrupadas por faixa nominal de 50.

## Shards
- `001a050.md` — faixa nominal `001a050`; cobre de `Paralela-1 — negação bitwise dual (`~` + `nope`) + MCP mínimo (histórico)` até `Paralela-1 — negação bitwise dual (`~` + `nope`) + MCP mínimo (histórico)`.
