pacote main;

trazer texto.buscar;

carinho principal() -> bombom {
    nova log: verso = "status=ok;code=42";
    nova pos_status: bombom = buscar(log, "status=");
    nova pos_ausente: bombom = buscar(log, "erro=");

    falar(pos_status);
    falar(pos_ausente);

    talvez pos_status == 0 {
        talvez pos_ausente == 18446744073709551615 {
            mimo 0;
        }
    }

    mimo 1;
}
