pacote fase157_formatacao_simples_saida_valido;

trazer texto.formatar, tamanho;

carinho principal() -> bombom {
    nova linha: verso = formatar("saldo={}", 42);
    falar(linha);
    falar(tamanho(linha));
    mimo 0;
}
