pacote main;

carinho principal() -> bombom {
    nova bytes: seta<u8> = alocar(16);
    nova desalinhado: seta<u64> = (bytes virar seta<bombom> + 1) virar seta<u64>;
    *desalinhado = 1;
    mimo 0;
}
