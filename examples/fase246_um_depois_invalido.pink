pacote main;

carinho principal() -> bombom {
    nova bytes: seta<u8> = alocar(8);
    nova depois: seta<u8> = (bytes virar seta<bombom> + 8) virar seta<u8>;
    nova valor: u8 = *depois;
    falar(valor);
    mimo 0;
}
