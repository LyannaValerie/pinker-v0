pacote main;

trazer lista.bombom_anexar, bombom_criar, bombom_definir, bombom_obter;

carinho principal() -> bombom {
    nova lista: lista<bombom> = bombom_criar();
    bombom_anexar(lista, 7);
    bombom_definir(lista, 0, 33);
    falar(bombom_obter(lista, 0));
    mimo 0;
}
