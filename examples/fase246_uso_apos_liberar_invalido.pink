pacote main;

trazer memoria.alocar;
trazer memoria.liberar;

carinho principal() -> bombom {
    nova bytes: seta<u8> = alocar(1);
    liberar(bytes);
    nova valor: u8 = *bytes;
    falar(valor);
    mimo 0;
}
