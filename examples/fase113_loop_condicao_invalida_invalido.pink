pacote main;

// Fase 125 abriu `>=` no recorte comparativo; divisão em condição de loop segue fora no backend externo.
carinho principal() -> bombom {
    nova muda x: bombom = 0;

    sempre que x / 2 < 5 {
        x = x + 1;
    }

    mimo x;
}
